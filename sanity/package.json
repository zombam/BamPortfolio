{
  "name": "bam-portfolio-studio",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "sanity dev",
    "start": "sanity start",
    "build": "sanity build",
    "deploy": "sanity deploy",
    "migrate": "node --env-file=.env scripts/migrate-portfolio.mjs"
  },
  "dependencies": {
    "@sanity/vision": "^3",
    "react": "^18",
    "react-dom": "^18",
    "sanity": "^3",
    "styled-components": "^6"
  },
  "devDependencies": {
    "@sanity/client": "^6"
  }
}
