SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production

# Write token — create at manage.sanity.io > API > Tokens (Editor permission is enough)
# Only needed locally to run the migration script. Never commit the real .env.
SANITY_MIGRATE_TOKEN=
